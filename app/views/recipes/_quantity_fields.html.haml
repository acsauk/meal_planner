.nested-fields
  = f.fields_for :ingredient do |quantity_ingredient|
    = quantity_ingredient.label :name, "Ingredient:"
    = quantity_ingredient.text_field :name

  = f.label :amount, "Amount:"
  = f.text_field :amount

  = f.label :unit, "Unit:"
  = f.text_field :unit

  = link_to_remove_association "Remove", f
